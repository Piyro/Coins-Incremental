namespace CoinTowerIdle.PassiveIncome
{
    public static class PassiveIncomeCalculator
    {
        //Note: This method is commented out because it is not used in the current implementation. It was originally intended to calculate the income per second
        //for a given passive asset instance, but the calculation is now handled directly within the PassiveAssetInstance class.
        /* public static double Calculate(
             PassiveAssetInstance asset)
         {
             return asset.IncomePerSecond;
         }*/
    }
}